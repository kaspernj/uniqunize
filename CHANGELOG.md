# Changelog
- Add inline source maps to the TypeScript build output.
- Use a single TypeScript config for build and typecheck scripts.
- Add a patch release script that builds, bumps the version, pushes, and publishes.
- Add CJS-compatible export conditions for bundlers that resolve via `require`.
