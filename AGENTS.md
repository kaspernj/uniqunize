# Agent Notes

- Create PRs using the GitHub CLI (`gh pr create`).
- Use short, descriptive branch names like `chore/esm-build-jsdoc-typecheck`.
- Prefer clear commit messages like `chore: switch to ESM build + JSDoc lint`.
